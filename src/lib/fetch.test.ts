import { describe, it } from 'vitest';
import { fetchSchedule, fetchVersions } from './fetch.js';

describe('fetch', () => {
  describe('fetchSchedule', () => {
    it('should fetch from the nodejs schedule', async () => {});
  });
});
